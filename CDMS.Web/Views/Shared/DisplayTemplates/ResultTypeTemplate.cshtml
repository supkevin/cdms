@model string

@{
   SelectListItem info = ((SelectList)ViewBag.ResultTypeList).Where(x => x.Value == Model).SingleOrDefault();    
}
<input class="form-control auto-complete-name text-box single-line"
  readOnly="ReadOnly" type="text" value="@( info == null ? "" : info.Text)" />  